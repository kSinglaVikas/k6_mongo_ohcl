apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-wrapper-config
  namespace: benchmark
data:
  DB_NAME: charts
  ONED_EQ_COLLECTION: oned-eq
  HISTORIC_EQ_COLLECTION: historic-eq
  DEBUG: "false"
  MAX_POOL_SIZE: "200"
  MIN_POOL_SIZE: "100"
