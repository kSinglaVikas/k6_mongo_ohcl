apiVersion: v1
kind: Service
metadata:
  name: mongo-wrapper
  namespace: benchmark
spec:
  type: NodePort
  selector:
    app: mongo-wrapper
  ports:
    - name: http
      port: 9010
      targetPort: 9000
      nodePort: 30090
